{
  "name": "lily-ui",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run build:config && ./node_modules/.bin/tailwindcss -c tailwind.config.js -i src/input.css -o dist/output.css --watch",
    "dev:machines": "rm -rf public/machines && bun build ./src/machines/*.ts --target browser --format esm --outdir public/machines --entry-naming [name].js --watch",
    "dev:all": "bash scripts/dev.sh",
    "build": "bun run build:config && ./node_modules/.bin/tailwindcss -c tailwind.config.js -i src/input.css -o dist/output.css --minify",
    "build:config": "bun build tailwind.config.ts --target node --outfile tailwind.config.js",
    "build:machines": "rm -rf public/machines && bun build ./src/machines/*.ts --target browser --format esm --outdir public/machines --entry-naming [name].js"
  },
  "devDependencies": {
    "@tailwindcss/cli": "4.1.17",
    "tailwindcss": "4.1.17",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "@radix-ui/colors": "^3.0.0",
    "alpinejs": "^3.15.2"
  }
}
